@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<link rel="stylesheet" type="text/css" href="~/css/index.css" />

<div id="Div">
    @if(Model.IsLoggedIn){
        <div id="mainDiv">
        @foreach (var item in @Model.boardList)
        {
            <div>
           
                <h5 onclick="location.href='/Board?id=@item.Id'">@item.Title</h5>
            
            </div>
            
        }


        <div id="newBoardForm">
            <form onsubmit="CreateBoard()">
                <input type="text" id="btitle" name="btitle" value="New board title">
                <input type="submit" id="submit-btn" name="submit-btn" value="Create">
            </form>
    </div>

        </div>
    }else if (@Model.IsLoggedIn == false && @Model.CreateNewAccount == false){
        <div id="mainDiv">
            <div>
                <h5>
                    Please sing in, or register a new account!
                    <br>
                </h5>
            </div>
        </div>
    }else if(@Model.IsLoggedIn == false && @Model.CreateNewAccount == true){

    }
    
</div>




 